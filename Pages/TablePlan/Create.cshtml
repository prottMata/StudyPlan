@page
@model StudyPlanProject.Pages.TablePlan.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Plan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Plan.IdDiscip" class="control-label"></label>
                <select asp-for="Plan.IdDiscip" class ="form-control" asp-items="ViewBag.IdDiscip"></select>
            </div>
            <div class="form-group">
                <label asp-for="Plan.IdSemestr" class="control-label"></label>
                <select asp-for="Plan.IdSemestr" class ="form-control" asp-items="ViewBag.IdSemestr"></select>
            </div>
            <div class="form-group">
                <label asp-for="Plan.IdTypeControl" class="control-label"></label>
                <select asp-for="Plan.IdTypeControl" class ="form-control" asp-items="ViewBag.IdTypeControl"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
